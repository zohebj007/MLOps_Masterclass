name: CI-CD Pipeline

on:
  push:
    branches:
      - cicd

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code ##
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Docker
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. Build Image
      - name: Build Docker Image
        run: |
          IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/mlops-app:latest
          ls -la ./Docker
          docker build -t $IMAGE_NAME -f ./Docker/DockerFile .

      # 4. Login to DockerHub
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5. Push Image
      - name: Push Docker Image
        run: |
          IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/mlops-app:latest
          docker push $IMAGE_NAME
      
      # - name: Test SSH key format
      #   run: |
      #    echo "${{ secrets.SSH_PRIVATE_KEY }}" | head -n 5
      #    ssh -i key.pem ubuntu@13.235.104.41 #  Test SSH connection to EC2 instance (replace with your instance's IP) 

      # 6. Deploy to EC2 via SSH
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_LOCAL_PRIVATE_KEY }}
          script: |
            set -e

            IMAGE_NAME=${{ secrets.DOCKERHUB_USERNAME }}/mlops-app:latest

            echo "===== Docker Login ====="
            echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

            echo "===== Pull Latest Image ====="
            docker pull $IMAGE_NAME

            echo "===== Stop Old Container ====="
            docker stop mlops-app-latest || true
            docker rm mlops-app-latest || true

            echo "===== Run New Container ====="
            docker run -d \
              --name mlops-app-latest \
              -p 5000:5000 \
              --restart always \
              zoheb007/mlops-app:latest

            echo "===== Cleanup Old Images ====="
            docker image prune -f